image: Visual Studio 2017

platform:
  - x64

cache:
  - node_modules
  - '%USERPROFILE%\.electron'
  - "%LOCALAPPDATA%\\Yarn"

install:
  - ps: Install-Product node 8 x64
  - yarn

branches:
  only:
  - master

for:

# TEST
-
  branches:
    only:
      - master
      - production
  test_script:
    - yarn test