language: node_js
node_js: "7"

matrix:
  include:
    - os: osx
      env:
        - ELECTRON_CACHE=$HOME/.cache/electron
        - ELECTRON_BUILDER_CACHE=$HOME/.cache/electron-builder

cache:
  directories:
  - node_modules
  - $HOME/.cache/electron
  - $HOME/.cache/electron-builder

script:
  - npm run test && npm run lint
  - if [ "$TRAVIS_BRANCH" = "master" ]; then npm run build; fi

before_deploy:
  # Set up git user name and tag this commit
  - git config --local user.name "Максим Ильин"
  - git config --local user.email "negamaxi@gmail.com"
  - git tag "$(date +'%Y%m%d%H%M%S')-$(git log --format=%h -1)"

before_deploy: ls -la dist/

deploy:
  provider: releases
  api_key: $GH_TOKEN
  file_glob: true # required to upload all files in a folder
  file: dist/**/*
  skip_cleanup: true
  on:
    branch: master
    condition: $TRAVIS_PULL_REQUEST = "false"